# ── Core framework ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
starlette>=0.40.0

# ── Validation ────────────────────────────────────────────────────────────────
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ── Data / async ──────────────────────────────────────────────────────────────
redis[hiredis]>=5.1.0
httpx>=0.27.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
PyYAML>=6.0

# ── Vector store client ───────────────────────────────────────────────────────
# chromadb >=1.x no longer requires chroma-hnswlib (C++ build) in client mode.
# The HttpClient is pure-Python; all embedding/indexing runs in the chromadb container.
chromadb>=0.5.15

# ── Filesystem watcher ────────────────────────────────────────────────────────
watchdog>=4.0.0

# ── Optional: LLM synthesis (uncomment one and set LLM_PROVIDER in .env) ─────
# anthropic>=0.30.0
# openai>=1.30.0

# ── Optional: local voice transcription (falls back to stub if not installed) ─
# openai-whisper
