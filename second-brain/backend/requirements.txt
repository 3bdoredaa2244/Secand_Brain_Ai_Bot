fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
redis[hiredis]==5.1.1
chromadb==0.5.15
sentence-transformers==3.1.1
python-dotenv==1.0.1
httpx==0.27.2
watchdog>=4.0.0
PyYAML>=6.0
python-multipart>=0.0.9

# Phase 2 — LLM synthesis (install the provider you use)
# pip install anthropic   # for LLM_PROVIDER=anthropic
# pip install openai      # for LLM_PROVIDER=openai

# Phase 2 — voice transcription (optional; endpoint works without it via stub)
# pip install openai-whisper
