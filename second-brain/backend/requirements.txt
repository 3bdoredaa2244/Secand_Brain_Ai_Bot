fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
redis[hiredis]==5.1.1
chromadb==0.5.15
python-dotenv==1.0.1
httpx==0.27.2
watchdog>=4.0.0
PyYAML>=6.0
python-multipart>=0.0.9

# Optional — LLM synthesis (add one if you set LLM_PROVIDER in .env)
# anthropic>=0.30.0
# openai>=1.30.0

# Optional — real voice transcription (endpoint works without it via stub)
# openai-whisper
