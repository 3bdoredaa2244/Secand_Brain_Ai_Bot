# ── Core framework ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
starlette>=0.40.0

# ── Validation ────────────────────────────────────────────────────────────────
pydantic>=2.9.0
pydantic-settings>=2.5.0

# ── Data / async ──────────────────────────────────────────────────────────────
redis[hiredis]>=5.1.0
httpx>=0.27.0
python-dotenv>=1.0.0
python-multipart>=0.0.9
PyYAML>=6.0

# ── Vector store client ───────────────────────────────────────────────────────
# chromadb >=1.x no longer requires chroma-hnswlib (C++ build) in client mode.
# The HttpClient is pure-Python; all embedding/indexing runs in the chromadb container.
chromadb>=0.5.15

# ── Filesystem watcher ────────────────────────────────────────────────────────
watchdog>=4.0.0

# ── Google APIs (Phase 3 — Gmail + Calendar) ─────────────────────────────────
# google-auth-oauthlib transitively requires `cryptography` which we also use
# directly for token encryption.
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.130.0
cryptography>=42.0.0

# ── Optional: LLM synthesis (uncomment one and set LLM_PROVIDER in .env) ─────
# anthropic>=0.30.0
# openai>=1.30.0

# ── Voice subsystem (Phase 3 — local STT + TTS) ──────────────────────────────
# Both packages fall back to stubs / no-op when missing; backend never crashes.
#
# STT: faster-whisper (CTranslate2-based, CPU-fast, has cp314 wheels)
#      Model auto-downloads on first transcription via HuggingFace.
faster-whisper>=1.2.0
#
# TTS: Piper (ONNX, CPU-only by default, ~63 MB voice model)
#      Voice model auto-downloads on POST /api/v1/voice/setup
piper-tts>=1.4.0
#
# Legacy: openai-whisper still supported by REST /voice/input endpoint
# openai-whisper
